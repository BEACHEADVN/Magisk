<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash dùng cho mọi API (Magisk style) -->
    <style name="SplashTheme" parent="Theme.SplashScreen.Common">
        <item name="android:windowBackground">
            @drawable/bg_splash
        </item>
    </style>

    <!-- Giữ lại cho code nội bộ -->
    <style name="StubSplashTheme" parent="SplashTheme"/>

</resources>
